<h1>Welcome!</h1>

<h3>Here are all the books in this database:</h3>

<% if @books.any?() %>
  <p>Click on an book to see information about them and mark which patron checks them out:</p>

  <ul>
    <% @books.each do |book| %>
      <li><a class="green" href="/books/<%= book.id() %>"><%= book.title() %></a></li>
    <% end %>
  </ul>
<% else %>
  <p>There are no books added yet!</p>
<% end %>

<p><a class="green" href="/books">Add a new book</a></p>


<h3>Here are all the patrons in this database:</h3>

<% if @patrons.any?() %>
  <p>Click on a patron to see what books they have checked out:</p>

  <ul>
    <% @patrons.each do |patron| %>
      <li><a class="green" href="/patrons/<%= patron.id() %>"><%= patron.name() %></a></li>
    <% end %>
  </ul>
<% else %>
  <p>There are no patrons added yet!</p>
<% end %>

<p><a class="green" href="/patrons">Add a new patron</a></p>
